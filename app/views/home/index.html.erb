<div class="row fullscreen">
	<div class="small-12 columns fullscreen">
		<div id="map-canvas"/>
	</div>
</div>

<% content_for :page_js do %>
	<%= javascript_include_tag "//maps.googleapis.com/maps/api/js?key=" + ENV["GOOGLE_MAPS_API_KEY"]%>
	<script type="text/javascript">
		(function(win){
			$(document).ready(function() {
				$.ajax({
					url: "/locations?lat=37.73461&lon=-122.39255",
					contentType: "application/json",
					success:function(data, textStaus, jqXHR) {
						console.log(data);
						var mapOptions = { center: new google.maps.LatLng(-34.397, 150.644), zoom: 4 };
				        var map = new google.maps.Map(document.getElementById("map-canvas"), mapOptions);
					}
				});
			});
		})(window);
	</script>
<% end %>